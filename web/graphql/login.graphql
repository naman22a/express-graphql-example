mutation Login($email: String!, $password: String!) {
    login(loginDto: { email: $email, password: $password }) {
        ok
        errors {
            field
            message
        }
    }
}
